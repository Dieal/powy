# Todos
+ Add visual improvements (borders, status line, row number, ecc.)
+ Add Dialog / Notification System: one more invasive (top corner notifications), the other on bottom row, less invasive (like vim)
+ IO Manager (Read and Write struct)
+ In insert mode, when going to new line and the cursor is in the middle of a row, break the row
+ To avoid going out of bounds, when a line is too long and the above one is shorter, position cursor on the last char of the 
+ Make some features (i.e bounds: should cursor move freely or be bounded by buffer and rows length?) configurable
